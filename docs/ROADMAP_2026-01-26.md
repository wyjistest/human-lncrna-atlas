# 未来 1–2 周路线图（可执行）

> **状态说明**: 本路线图为 2026-01-26 快照，现状以 `docs/CURRENT_STATUS.md` 为准。

目标：在 Phase 3.x 现有能力基础上，把“回归可对比、性能可解释、数据可扩展”变成稳定工作流（小步提交、可审计可回滚）。

## 1) 本周（1 周）优先级：性能回归对比再增强

### 1.1 Admin metrics diff 补齐 endpoints P99（P1）
- 目标：`scripts/admin_metrics_snapshot.py --compare` 额外输出 `Response P99` 与 `DB P99` 的端点变化榜单（Top regressions / improvements / other changes）。
- ✅ 进展：已在 `scripts/admin_metrics_snapshot.py --compare` 中补齐 `Top endpoint changes by Response P99` 与 `Top endpoint changes by DB P99` 两个章节，并补齐单测防回归。
- 验收标准：
  - diff 报告包含 `Top endpoint changes by Response P99` 与 `Top endpoint changes by DB P99` 两个章节；
  - 字段缺失/样本不足时降级输出，不抛异常；
  - 单测覆盖并在本地 `./scripts/run-tests.sh ci` 通过。

### 1.2 Cache diff 展示优先级优化（P2，可选）
- 目标：对 cache routes/keys/namespaces 的变化排序规则做优化（优先展示变化最大的项，而不是按 key 字典序）。
- ✅ 进展：cache routes/keys/namespaces diff 已按变化幅度排序（compute_count/avg/max、hit_rate、requests），输出更“可定位”，且顺序稳定便于对比。
- 验收标准：diff 输出更“可定位”，且不破坏现有字段兼容性。

## 2) 下周（2 周）方向：体验与研究产出

### 2.1 前端 bundle 体积与首屏体验（P2）
- 目标：针对 `igv-vendor` / `antd-vendor` 等大 chunk，评估并落地“按页面延迟加载/拆包”，降低首屏成本。
- ✅ 进展：已补齐首屏预加载护栏（`frontend/web/scripts/check-entry-preloads.mjs`），修复 `pdf-vendor` 被首屏误拉起问题（`frontend/web/vite.config.ts`），并将 Overlap 页 IGV 默认收起 + 延迟加载（避免进入页面即加载 `igv-vendor`）。
- 验收标准：`npm run build` 通过；性能基线（E2E/perf 或本地指标）可对比。

### 2.2 Phase 6.0（Research Analysis）落地最小产出（P2）
- 目标：把 `docs/PHASE_6.0_RESEARCH_ANALYSIS_PLAN.md` 中的分析方向落地成“可重复运行”的最小产出（脚本/Notebook/导出物）。
- 建议最小交付：高亲和力（BA>100）Top lncRNA 榜单（CSV/Markdown）+ 可视化图片（可选）。
- ✅ 进展：已提供脚本入口 `scripts/research/top_lncrna_by_binding_affinity.py`，并扩展补齐（Top lncRNA 靶基因导出、跨物种保守性分层统计）；Phase 6.0 文档已补齐可复现命令与产物路径。
- 验收标准：提供 1 条可复现命令/入口 + 清晰的输入/输出说明（不强依赖特定机器路径）。

## 3) 执行与回滚策略

- 每个主题独立 commit（必要时独立分支/PR），确保可 `git revert <sha>` 回滚。
- 先对齐“证据与对比”：优先补齐可观测/可回归工具，再做性能/体验优化。
