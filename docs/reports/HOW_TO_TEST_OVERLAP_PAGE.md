# 如何测试 lncRNA-ChIP-seq Overlap 页面

> 更新（2026-01-24）：本文档为手工测试步骤与检查清单模板（可复制后逐项勾选），不代表当前开发待办；现状以 `docs/CURRENT_STATUS.md` 为准。

## 快速测试指南

### 前提条件
- 后端服务已启动（端口 8000）
- 前端开发服务器已启动（默认端口 5173；若被占用，Vite 可能自动使用 5174）

### 访问页面

#### 方法 1: 通过导航菜单
1. 访问首页: http://localhost:5173
2. 在左侧导航菜单中找到 "lncRNA-ChIP-seq 重叠" （或 "lncRNA-ChIP-seq Overlap"）
3. 点击菜单项进入页面

#### 方法 2: 直接访问
直接在浏览器中打开: http://localhost:5173/lncrna-chipseq-overlap

### 功能测试清单

#### 1. 页面加载测试
- [ ] 页面标题显示正确
- [ ] 面包屑导航显示: Home → Regulations → lncRNA-ChIP-seq Overlap
- [ ] 页面描述文本显示正确
- [ ] 主组件（表格和筛选面板）加载成功

#### 2. 国际化测试
- [ ] 点击右上角语言切换按钮
- [ ] 切换到英文，所有文本变为英文
- [ ] 切换到中文，所有文本变为中文
- [ ] 确认无翻译键泄漏（如 "overlap.page.title"）

#### 3. 筛选面板测试
- [ ] 点击"高级筛选"展开筛选面板
- [ ] Mark 类型选择器正常工作
- [ ] 细胞系选择器正常工作
- [ ] 染色体选择器正常工作
- [ ] 数值筛选器（最小重叠长度等）可以输入
- [ ] 点击"重置"按钮清空筛选条件

#### 4. 数据表格测试
- [ ] 表格显示列标题（lncRNA, 靶基因, Mark, 细胞系等）
- [ ] 表格数据正常显示
- [ ] 分页器正常工作
- [ ] 排序功能正常（点击列标题）
- [ ] 行选择功能正常

#### 5. 统计卡片测试
- [ ] 统计卡片显示总重叠数
- [ ] 统计卡片显示涉及的 lncRNA 数量
- [ ] 统计卡片显示涉及的靶基因数量
- [ ] 平均值统计显示正确

#### 6. 导出功能测试
- [ ] 导出 CSV 按钮可点击
- [ ] 导出 BED 按钮可点击
- [ ] 提示信息正确显示

#### 7. 响应式设计测试
- [ ] 调整浏览器窗口大小，布局自动调整
- [ ] 在小屏幕上，组件适当折叠或重排
- [ ] 移动端视图正常工作

#### 8. 错误处理测试
- [ ] 网络错误时显示友好错误信息
- [ ] 数据为空时显示空状态提示
- [ ] 加载状态显示正确（Loading spinner）

### 浏览器兼容性测试

测试以下浏览器：
- [ ] Chrome/Edge (最新版)
- [ ] Firefox (最新版)
- [ ] Safari (最新版)

### 性能测试

- [ ] 首次加载时间 < 3 秒
- [ ] 筛选操作响应 < 500ms
- [ ] 分页操作响应 < 300ms
- [ ] 无明显卡顿或延迟

### 已知问题

1. **后端 API 已实现**：主查询端点为 `/api/v1/lncrna-chipseq-overlap`（更多端点见 `frontend/backend/app/routers/lncrna_chipseq_overlap.py`）
2. **样例库可能无 overlap 数据**：若样例数据库缺少 ChIP-seq peaks/overlap 相关表，页面可能显示空结果（这是预期的“优雅降级”）
3. **导出与大数据量**：导出已支持 CSV/BED；真实数据量较大时建议先收窄筛选条件并设置合理的 `max_rows`

### 截图建议

建议截取以下界面：
1. 完整页面（中文界面）
2. 完整页面（英文界面）
3. 筛选面板展开状态
4. 统计卡片特写
5. 数据表格特写
6. 移动端视图

### 报告问题

如果发现任何问题，请记录：
- 问题描述
- 重现步骤
- 浏览器和版本
- 截图（如适用）
- 控制台错误信息（按 F12 查看）

### 控制台命令

检查错误：
```bash
# 查看前端日志
tail -f /tmp/frontend.log

# 查看后端日志
tail -f /tmp/backend.log

# 检查服务状态
curl http://localhost:8000/health
curl http://localhost:5173
```

---

**测试完成标准**:
- 所有核心功能正常工作
- 中英文切换无误
- 无控制台错误
- 用户体验流畅

**预估测试时间**: 15-20 分钟
