# 2025-12-02 监控仪表板与分页索引优化

## 🎯 系统监控仪表板（重大功能）

实现完整的 Prometheus 风格监控可视化系统，分三个阶段完成：

### Phase 1: 基础监控
- 新增 `/api/v1/admin/metrics` 端点
- 前端 4 个基础卡片：请求数、错误率、平均响应时间、系统状态
- 5 秒自动刷新

### Phase 2: 增强分析
- 响应时间分布直方图（ECharts BarChart）
- 错误率趋势折线图（最近 10 分钟）
- 端点统计表格（Top 20，支持排序）
- 滑动窗口数据收集（deque）

### Phase 3: 高级监控
- 系统资源监控：CPU/内存/磁盘使用率（psutil）
- ECharts GaugeChart 仪表盘展示
- 告警规则 + 横幅展示
- P50/P95/P99 响应时间百分位

### 新增文件

**后端**:
| 文件 | 说明 |
|------|------|
| `app/routers/admin.py` | 监控 API 路由 |
| `app/schemas/monitoring.py` | 完整监控数据模型 |
| `requirements.txt` | 添加 `psutil>=5.9.0` |

**前端**:
| 文件 | 说明 |
|------|------|
| `pages/Admin/Monitoring/index.tsx` | 监控主页面 |
| `components/ResponseTimeChart.tsx` | 响应时间分布图 |
| `components/ErrorTrendChart.tsx` | 错误率趋势图 |
| `components/EndpointTable.tsx` | 端点统计表 |
| `components/SystemGauge.tsx` | CPU/内存仪表盘 |
| `components/AlertsBanner.tsx` | 告警横幅 |
| `components/PercentilesCard.tsx` | 百分位卡片 |
| `utils/echarts.ts` | 添加 LineChart, GaugeChart |
| `types/monitoring.ts` | 监控类型定义 |

### 访问方式
- **监控页面**: http://localhost:5173/admin/monitoring
- **API 端点**: http://localhost:8000/api/v1/admin/metrics

---

## 🔧 后端分页稳定性修复

修复分页查询缺少 ORDER BY 导致结果不稳定的问题：

| 文件 | 位置 | 修复说明 |
|------|------|----------|
| `genes.py` | 第107行 | 添加 `order_by(Gene.gene_id)` |
| `diseases.py` | 第93行 | 添加 `order_by(Trait.trait_id, Ontology.ontology_id)` |
| `diseases.py` | 第243行 | 添加 `order_by(TraitGeneAssociation.association_id)` |

## 📊 数据库索引优化

新增3个覆盖索引，优化分页排序查询性能：

| 索引名 | 表 | 定义 | 大小 |
|--------|-----|------|------|
| `idx_reg_ba_id` | regulations | `(binding_affinity DESC, regulation_id)` | 24 MB |
| `idx_reg_species_ba_id` | regulations | `(species_id, binding_affinity DESC, regulation_id)` | 31 MB |
| `idx_tga_trait_assoc` | trait_gene_associations | `(trait_id, association_id)` | 1.5 MB |

**索引效果验证**:
```sql
EXPLAIN SELECT * FROM regulations ORDER BY binding_affinity DESC LIMIT 100;
→ Index Scan using idx_reg_ba_id on regulations  ✅
```

## ⚡ 前端缓存优化

**React Query 全局配置** (`src/main.tsx`):
```typescript
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000,  // 5分钟内数据视为新鲜
      gcTime: 10 * 60 * 1000,    // 10分钟后垃圾回收
      refetchOnWindowFocus: false,
    },
  },
})
```

**相邻页预加载**:
- 新增 `usePrefetchGenes` Hook (`src/hooks/useGenes.ts`)
- 新增 `usePrefetchRegulations` Hook (`src/hooks/useRegulations.ts`)
- Genes 页面和 Regulations 页面添加自动预加载逻辑

## 🐛 TypeScript 类型修复

修复 Cytoscape EventObject 类型缺失问题：

| 文件 | 修复说明 |
|------|----------|
| `src/types/cytoscape-ext.d.ts` | 添加 `EventObject` 接口定义，包含事件属性和方法 |

**修改前错误**:
```
error TS2694: Namespace '"cytoscape"' has no exported member 'EventObject'
```

**修改后**: 构建成功 ✅

---

## 🔒 安全与性能修复（Codex 代码审查）

通过 Codex CLI 进行全面代码审查，发现并修复 10 个问题。

### 安全修复

#### 1. Admin API 鉴权 (高危)
**文件**: `frontend/backend/app/routers/admin.py`

| 修复前 | 修复后 |
|--------|--------|
| `/api/v1/admin/*` 无任何鉴权 | IP 白名单 + 可选 API Key 认证 |

```python
# 新增 verify_admin_access 依赖注入
async def verify_admin_access(request: Request):
    # 允许 localhost + 内网 IP (192.168.x.x, 10.x.x.x)
    # 支持可选 X-Admin-API-Key 头认证
```

**配置更新** (`config.py`):
- `ADMIN_API_KEY`: 环境变量配置
- `ADMIN_ALLOWED_IPS`: IP 白名单

#### 2. ETL 硬编码敏感信息移除
**文件**: `etl/import_sequences.py`

| 修复前 | 修复后 |
|--------|--------|
| 数据库密码/用户名硬编码 | 环境变量 + CLI 参数 |

```bash
# 新用法
export DB_HOST=localhost DB_USER=amax DB_NAME=lncrna_production
python3 import_sequences.py --species 1 --dry-run
```

### 性能修复

#### 3. 基因网络接口无上限 (中危)
**文件**: `frontend/backend/app/routers/network.py:249-334`

| 修复前 | 修复后 |
|--------|--------|
| `query_1st.all()` 无限制 | 添加 `max_edges` 参数 (默认 500, 最大 5000) |

```python
@router.get("/gene/{gene_id}")
async def get_gene_network(
    gene_id: int,
    max_edges: int = Query(500, ge=1, le=5000)  # 新增
):
    # 返回 truncated 标志告知前端数据被截断
```

#### 4. 跨物种比较全表扫描 (中危)
**文件**: `frontend/backend/app/routers/network.py:411-457`

| 修复前 | 修复后 |
|--------|--------|
| 每个同源基因全表扫描 | 添加 `max_targets_per_species` 参数 (默认 100) |

### 代码质量修复

#### 5. ETL 资源泄漏
**文件**: `etl/import_sequences.py`

- 使用 `@contextmanager` 管理 cursor
- 事务包裹支持回滚
- 完善异常处理

#### 6. ETL 重复导入无去重
**文件**: `etl/import_regulations.py`

- 使用 `ON CONFLICT DO NOTHING`
- 基于 7 字段组合唯一键去重

**新增迁移文件**: `etl/migrations/001_add_regulations_unique_constraint.sql`
```sql
CREATE UNIQUE INDEX idx_regulations_unique_key ON regulations (
    species_id, lncrna_gene_id, target_gene_id,
    lncrna_start, lncrna_end, dna_start, dna_end
);
```

#### 7. React Regulations 无效重渲染
**文件**: `frontend/web/src/pages/Regulations/index.tsx`

| 修复前 | 修复后 |
|--------|--------|
| `apiParams` 每次渲染新建 | 使用 `useMemo` 固定引用 |

```typescript
const apiParams = useMemo(() => ({
  page, page_size: pageSize, ...filters
}), [page, pageSize, filters.min_ba, ...])
```

#### 8. useRegulationDetail 弹窗关闭后仍请求
**文件**: `frontend/web/src/hooks/useRegulations.ts`

| 修复前 | 修复后 |
|--------|--------|
| 仅检查 `regulationId` | 添加 `enabled` 参数控制 |

```typescript
// Hook 定义
export const useRegulationDetail = (
  regulationId: number | null,
  options?: { enabled?: boolean }
)

// 调用方式 (SequenceViewer.tsx)
useRegulationDetail(regulationId, { enabled: open })
```

#### 9. 清理敏感文件
- 删除 `frontend/backend/lncrna_atlas.db` (空文件)
- `.gitignore` 已配置 `*.db` 规则

### 测试验证

| 测试类型 | 结果 |
|----------|------|
| 后端 API 测试 (pytest) | 14/14 passed |
| 前端单元测试 (vitest) | 6/6 passed |
| Admin API 鉴权验证 | ✅ localhost 访问正常 |
| 网络接口 max_edges 验证 | ✅ 参数生效 |

### 数据库迁移

已执行迁移脚本，创建唯一约束索引：
```bash
psql -U amax -d lncrna_production -f etl/migrations/001_add_regulations_unique_constraint.sql
# ✅ CREATE INDEX idx_regulations_unique_key
```
