# Development / Test dependencies
# ==================================
# Usage:
#   pip install -r requirements-dev.txt -c constraints.txt
#
# This file includes runtime dependencies from requirements.txt, plus tools used
# in CI and local development (linting / unit tests).

-r requirements.txt

# 代码质量
ruff==0.15.2

# 测试
pytest==9.0.2
pytest-asyncio==1.3.0
# Starlette TestClient 依赖 httpx；与较新的 FastAPI/Starlette 组合时需保持兼容版本
# 参考：Starlette "full" extra 要求 httpx>=0.27.0
httpx>=0.27.0,<0.29.0
