/**
 * MSW API Handlers
 *
 * 状态说明：
 * - 所有 Stats API 已实现，Mock 已移除
 * - 所有 Regulations 筛选参数已实现
 *
 * 保留 MSW 配置用于未来可能的新 API Mock
 */

// import { http, HttpResponse } from 'msw'

export const handlers = [
  // 所有 API 已实现，无需 Mock
  // 保留空数组以便未来扩展
]
